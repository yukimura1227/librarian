%h1 Listing books

%table.uk-table.uk-table-divider.uk-table-striped.uk-table-hover.uk-table-small
  %thead
    %tr
      %th.uk-width-expand  Title
      %th.uk-width-shrink  Location
      %th.uk-width-shrink 

  %tbody
    - @books.each do |book|
      %tr
        %td= book.title
        %td= book.location
        %td= link_to 'Edit', edit_book_path(book)

